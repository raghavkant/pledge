---
import { url } from '../lib/url';

interface Props { path: string }
const { path } = Astro.props;
const links: { path: string; label: string; wide?: boolean }[] = [
  { path: 'australia/', label: 'Guides' },
  { path: 'australia/practice-test/', label: 'Practice test' },
  { path: 'support/', label: 'Support', wide: true },
];
---
<header class="site-header">
  <div class="container">
    <a class="brand" href={url()} aria-label="Pledge home">
      <img src={url('mark.svg')} alt="" width="30" height="30" />
      <span>Pledge</span>
    </a>
    <nav class="site-nav" aria-label="Main">
      <ul>
        {links.map((l) => (
          <li class:list={[{ 'nav-wide': l.wide }]}><a href={url(l.path)} aria-current={path === l.path ? 'page' : undefined}>{l.label}</a></li>
        ))}
      </ul>
    </nav>
  </div>
</header>
